const SHELL='chess-room-__BUILD_ID__';
self.addEventListener('install',event=>event.waitUntil((async()=>{const response=await fetch('/shell-assets.json',{cache:'no-store'});const assets=await response.json();const cache=await caches.open(SHELL);await cache.addAll(assets);})()));
self.addEventListener('activate',event=>event.waitUntil(self.clients.claim()));
self.addEventListener('message',event=>{if(event.data==='ACTIVATE')self.skipWaiting();});
self.addEventListener('fetch',event=>{if(event.request.method!=='GET'||new URL(event.request.url).origin!==self.location.origin)return;event.respondWith((async()=>{const url=new URL(event.request.url);if(url.pathname.startsWith('/engine/')){for(const name of await caches.keys())if(name.startsWith('engine-')&&!name.endsWith('-staging')){const cache=await caches.open(name);if(await cache.match('/offline-ready')){const match=await cache.match(event.request);if(match)return match;}}return fetch(event.request);}const shell=await caches.open(SHELL);const cached=await shell.match(event.request);if(cached)return cached;try{return await fetch(event.request);}catch(error){if(event.request.mode==='navigate'){const index=await shell.match('/index.html');if(index)return index;}throw error;}})());});
